const num1 = 2;
const num2 = 31;
const multiply = (num1 * num2); 

const random = Math.floor(Math.random() * 10) + 1;

const num3 = 4;
const num4 = 32;
const mod = (num3 % num4);

const max = Math.max(20);

console.log(Math.max(20));

